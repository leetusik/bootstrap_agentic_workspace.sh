# Python bytecode from running scripts/workflow.py
__pycache__/
*.pyc

# Executor-tier config (executors.toml, seeded with commented defaults) is
# deliberately tracked, not ignored — it holds no secrets and committing it
# shares the tier setup. In this upstream repo keep it at the commented
# defaults: a non-default sync-agents run mutates the committed agent files.
