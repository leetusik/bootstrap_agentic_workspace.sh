interface:
  display_name: "archive-phase"
  short_description: "Archive review-passed phases — batched at the end via archive-all (single-phase is a manual escape hatch)."
  default_prompt: "Use the archive-phase skill."
policy:
  allow_implicit_invocation: false
