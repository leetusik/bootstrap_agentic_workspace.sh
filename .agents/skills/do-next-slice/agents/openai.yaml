interface:
  display_name: "do-next-slice"
  short_description: "Continue the active phase by completing exactly one slice in automatic mode, then stop."
  default_prompt: "Use the do-next-slice skill in automatic mode."
policy:
  allow_implicit_invocation: false
