# Result

- Phase ID: P5
- Slice ID: P5.S1
- Slice: Make /explain opt-in at install (--with-explain)
- Review status: pending
- Next action: P5.REVIEW validates the phase and consolidates docs

## Outcome

Implemented exactly per `plan.md` (design reference: `phase.md` Findings & Notes):

1. **`installer/wrapper.sh`** — added the boolean `--with-explain` flag mirroring the
   `--force-empty-ok` / `--update` pattern: usage line (after `--dry-run`), `with_explain=0`
   init, `--with-explain) with_explain=1; shift ;;` arg-parse case, and
   `export WITH_EXPLAIN="$with_explain"`.
2. **`installer/main.py`** — inserted the install-time gate immediately after
   `CLAUDE_SKILLS`/`CODEX_SKILLS` are derived (the exact block from `phase.md`: `WITH_EXPLAIN`
   env read, `--update` presence-preservation `if`, `OPTIONAL_SKILLS`, `_excluded`, the two
   filtered list comprehensions). Confirmed `UPDATE` and `ROOT` are defined above the insertion
   point. No other edits to the managed lists, conflict guard, dir creation, skill-write loop,
   or `flag_stale_skills` — they all inherit the filtered lists. Bumped `WORKSPACE_VERSION`
   1 → 2.
3. **`CHANGELOG.md`** — added the `## v2 — 2026-07-02` entry (opt-in `/explain`; update
   preserves choice; migration notes: none) between the intro and `## v1`, newest-first.
4. **Rebuilt the root artifact** with `python3 installer/build.py` (never hand-edited);
   `--check` confirms artifact/source sync.
5. **`tests/retrofit_smoke.sh`** — additive only: two new asserts at the end of Test 5
   (default fresh install omits `.claude/skills/explain` and `.agents/skills/explain`) and a
   new "Test 8 — /explain is opt-in" (fresh install with `--with-explain` → exit 0,
   `workflow.py validate` passes, and `.claude/skills/explain/SKILL.md`,
   `.agents/skills/explain/SKILL.md`, `.agents/skills/explain/agents/openai.yaml` present and
   byte-matching the live repo copies via `diff -q`). Existing tests untouched; live
   `.claude/skills/explain` / `.agents/skills/explain` files untouched.

## Deviations from Plan

- Cosmetic only: the gating block in `phase.md` aligned `CODEX_SKILLS  =` with a double
  space; written single-space (`CODEX_SKILLS = ...`), normal Python style. Functionally
  identical.
- One check beyond the plan's list: the `--update` preservation path is not covered by the
  smoke suite, so it was sanity-checked manually against throwaway dirs (see Validation Run;
  the script lives in the session scratchpad, not the repo).

## Validation Run

- `python3 installer/build.py` → wrote `bootstrap_agentic_workspace.sh` (212930 bytes)
- `python3 installer/build.py --check` → `OK: bootstrap_agentic_workspace.sh is in sync with installer/ source`
- `bash tests/retrofit_smoke.sh` → `ALL RETROFIT SMOKE TESTS PASSED` (all 8 tests: Tests 1–7
  existing incl. the 2 new Test 5 asserts, plus all 8 Test 8 checks — opt-in presence +
  byte-match dual-apply for the three explain files)
- `python3 scripts/workflow.py validate` → `Workflow validation passed.`
- Manual `--update` sanity (throwaway dirs): (A) install `--with-explain` then `--update`
  without the flag → explain preserved and not flagged stale; (B) install without explain then
  `--update` → explain not added; (C) `--update --with-explain` → explain added. ALL PASS.

## Files Changed

- `installer/wrapper.sh`
- `installer/main.py`
- `CHANGELOG.md`
- `bootstrap_agentic_workspace.sh` (regenerated by `installer/build.py`)
- `tests/retrofit_smoke.sh`

## Doc Versions Created

- None (versioned at P5.REVIEW; "Doc impact" list in `phase.md` already covers operations +
  decisions — nothing new to append)

## Roadmap Updates

- None (the public-users half stays deferred per `intent.md`)

## Retrospective

- The single-filter-point design held: no downstream consumer of
  `CLAUDE_SKILLS`/`CODEX_SKILLS` needed touching, and Test 6's hardcoded allowlist needed no
  change, exactly as DECOMP predicted.
