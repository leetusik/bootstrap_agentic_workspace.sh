# P14.S2 result — Codex-native visual cowork implemented

The live Codex `design-cowork` package now implements the S1-selected built-in ImageGen/exact-reference
workflow as a self-contained flat skill. The deterministic installer was rebuilt from those live files,
and the Claude skill remained byte-for-byte unchanged at the required Git blob.

## Implemented contract

- Replaced the compatibility-oriented Codex body with a Codex-native, model-invocable product-visual-
  design guide while keeping schema/API/architecture work out of scope and keeping `co-work`
  main-thread-only, high risk, implementation-free, and undispatched.
- Defined the complete normal loop: decision-free handoff, built-in ImageGen or one exact approved
  reference, repository copy, exact `view_image` read-back, review-ready commit, one normal `pending`
  signoff, literal approval/hash recheck/`SIGNOFF.md`, gate-close commit, and separate `DECOMP2` work.
- Retained the one-vs-two-phase intake choice, one visual round per `co-work` slice, two-pass mixed-phase
  decomposition, single-pass design-only/apply phases, immutable superseding revision rounds, and
  ordinary bounded fidelity fixes.
- Added per-run probes and explicit `operator_need` halts for built-in generation, exact references,
  workspace read-back, optional explicitly chosen Figma input, project-native browser/E2E, and the
  `npx` plus bundled Playwright fallback. No silent retry, install, service switch, or credentialed CLI
  fallback is allowed.
- Specified the decision-free handoff and durable
  `docs/reference/design/rounds/<NN>-<slug>/` record, including `record.json`,
  `implementation-contract.md`, `validation.md`, approval-only `SIGNOFF.md`, and later fidelity
  evidence. The manifest/contract fields, path/hash checks, concreteness bar, untrusted-data boundary,
  one-gate state machine, and `RESPECT THE DESIGN` browser-fidelity contract are complete.
- Updated Codex metadata to describe ImageGen/exact-reference cowork, one operator signoff, and later
  browser fidelity while preserving `policy.allow_implicit_invocation: true`.
- Rebuilt `bootstrap_agentic_workspace.sh`; its payload contains the exact new Codex skill and metadata
  and the exact unchanged Claude skill.

## Files changed

- `.agents/skills/design-cowork/SKILL.md`
- `.agents/skills/design-cowork/agents/openai.yaml`
- `bootstrap_agentic_workspace.sh` (generated by `python3 installer/build.py`)
- `works/phases/active/P14/phase.md`
- `works/phases/active/P14/slices/P14.S2/result.md`

Orchestrator-owned workflow state files were already changing at slice start and were not edited by
this executor. No S3-owned orchestration/contract/executor surface, test, durable doc, release version,
changelog, README, installer source, or Claude skill was edited. No workflow transition, commit, push,
plugin install, image generation, browser run, or external write was performed.

## Validation

- `python3 scripts/workflow.py validate` — passed.
- `python3 installer/build.py --check` — passed; the committed generated artifact matches installer
  source inputs.
- `git hash-object .claude/skills/design-cowork/SKILL.md` — returned
  `0e3a1766ebb85126ab97356f4fdbc5f82753067e`, exactly the required preserved blob.
- Focused Python assertions over the Codex body — passed 12 requirement groups covering ImageGen,
  exact approved-reference and Figma-optional handling, repository persistence, durable record files,
  exact `view_image` read-back, one-signoff/pending lifecycle, capability halts, `DECOMP2`, project-
  browser/Playwright fidelity, immutable revision rounds, untrusted-data handling, and
  `RESPECT THE DESIGN`; also confirmed the body contains none of the three forbidden Claude-specific
  terms from the plan.
- Focused metadata assertions — passed for the new Codex description and
  `allow_implicit_invocation: true`.
- Focused installer assertions — passed exact payload equality for the new Codex body, new Codex
  metadata, and unchanged Claude body.
- `git diff --check` — passed.

## Phase notes and doc impact

Appended a P14.S2 implementation finding recording the landed Codex package, installer embedding, and
preserved Claude blob. No second Doc impact line was added: S1's existing `decisions`/`operations` line
already fully covers the implemented durable truth—Codex built-in ImageGen or exact reference, durable
read-back, one normal signoff with capability halts, separate `DECOMP2` implementation, real-browser
fidelity, and preservation of the other harness's visual cowork path. No doc version was created.

## Deviations

None.
